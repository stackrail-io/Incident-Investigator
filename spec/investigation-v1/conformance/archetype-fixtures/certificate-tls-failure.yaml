archetype_id: certificate-tls-failure
scenario_id: SC-ARCH-009
tier: core
description: Expired TLS certificate.
start:
    question: Why is the API gateway rejecting requests?
    service: api-gateway
    goal: root_cause
    time_window:
        start: "2026-06-27T09:00:00Z"
        end: "2026-06-27T09:30:00Z"
expect_after_start:
    required_evidence_min: 1
    plan_questions_min: 1
evidence_batches:
    - - id: sec-1
        timestamp: "2026-06-27T09:00:00Z"
        category: security_events
        entity: api-gateway
        summary: TLS certificate for api.example.com expired (x509)
    - - id: log-1
        timestamp: "2026-06-27T09:01:00Z"
        category: application_logs
        entity: api-gateway
        summary: 'x509: certificate has expired; TLS handshake failure'
expect_after_all_evidence:
    leading_hypothesis_id: hypothesis-certificate-expiry
    min_confidence: 18
    min_lead_margin: 3
    must_not_lead:
        - hypothesis-unknown
    min_hypotheses: 1
    graph_nodes_min: 1
expect_after_finish:
    state: completed
    report_required_fields:
        - executive_summary
        - hypotheses
        - confidence
        - postmortem
